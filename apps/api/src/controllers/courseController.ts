/**
 * Controller to handle course-related operations
 * @module controllers/courseController
 */

import { Request, Response } from 'express'

/**
 * Fetch the list of all available courses.
 * @route Pubic GET /api/courses
 * @group Courses - operation realted to course
 * @returns {Object[]} An array of course object
 */
const getCourses = async (req:Request, res: Response) => {

}